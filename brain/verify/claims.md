# Claims index (deterministic)

Append one row per claim. `key` = entity/compound/topic. Before adding, diff against existing rows with
the same key; if a new claim contradicts an existing one, open a contradiction report in this folder and
do not silently file. (CONVENTIONS §5.)

| key | claim | confidence | source | note-file |
|---|---|---|---|---|
| _seed_ | claims index initialized | established | brain build P0 | — |
