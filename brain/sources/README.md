# sources/ — inbox (raw, unprocessed)
Drop raw dumps here: pasted text, link notes, PDF→text. Append-only. INGEST (CONVENTIONS §3) turns
these into atomic notes/. No secrets/tokens; review PHI before commit.
