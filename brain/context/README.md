# context/ — durable, loads-on-demand
Identity + per-domain durable knowledge. The "answer the delta" layer. Writes here are HUMAN-GATED:
an agent proposes, you approve. Clinical state lives in /STATE_Roman.md; protocol.md references it.
